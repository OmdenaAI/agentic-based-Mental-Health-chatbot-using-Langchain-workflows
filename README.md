In this project we build an agentic question answering chatbot to support help seeking for individual with a mental health condition.

The chatbot uses CrewAI framework with mem0 as vector database. The app uses streamlit as a UI.

It has also been integrated with fastapi and packaged into a docker container.
